import React from 'react';
import {BtnContainer, TextBox, SearchBox} from './ButtonElements';

const Button = () => {
  return (
    <BtnContainer>
      <TextBox></TextBox>
      <SearchBox></SearchBox>
    </BtnContainer>
  )
}

export default Button;